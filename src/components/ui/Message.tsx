import styled from '@emotion/styled';

const Message = styled.span`
  font-size: 30px;
  grid-column: span 3;
  align-self: center;
  justify-self: center;
  color: white;
`;

export default Message;
